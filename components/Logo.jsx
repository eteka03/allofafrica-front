import { Box } from '@chakra-ui/react'
import React from 'react'
import { Text } from "@chakra-ui/react"
const Logo = (props) => {
    return (
       <Box {...props}>
           <Text>
               Logo
           </Text>
       </Box>
    )
}

export default Logo
